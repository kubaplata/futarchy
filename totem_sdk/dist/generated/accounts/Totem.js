/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';
export const totemDiscriminator = [83, 175, 123, 79, 123, 47, 196, 36];
/**
 * Holds the data for the {@link Totem} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Totem {
    constructor(dao, slotsPerChallengePeriod, admin, statements, requests, totalDisputes) {
        this.dao = dao;
        this.slotsPerChallengePeriod = slotsPerChallengePeriod;
        this.admin = admin;
        this.statements = statements;
        this.requests = requests;
        this.totalDisputes = totalDisputes;
    }
    /**
     * Creates a {@link Totem} instance from the provided args.
     */
    static fromArgs(args) {
        return new Totem(args.dao, args.slotsPerChallengePeriod, args.admin, args.statements, args.requests, args.totalDisputes);
    }
    /**
     * Deserializes the {@link Totem} from the data of the provided {@link web3.AccountInfo}.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static fromAccountInfo(accountInfo, offset = 0) {
        return Totem.deserialize(accountInfo.data, offset);
    }
    /**
     * Retrieves the account info from the provided address and deserializes
     * the {@link Totem} from its data.
     *
     * @throws Error if no account info is found at the address or if deserialization fails
     */
    static fromAccountAddress(connection, address, commitmentOrConfig) {
        return __awaiter(this, void 0, void 0, function* () {
            const accountInfo = yield connection.getAccountInfo(address, commitmentOrConfig);
            if (accountInfo == null) {
                throw new Error(`Unable to find Totem account at ${address}`);
            }
            return Totem.fromAccountInfo(accountInfo, 0)[0];
        });
    }
    /**
     * Provides a {@link web3.Connection.getProgramAccounts} config builder,
     * to fetch accounts matching filters that can be specified via that builder.
     *
     * @param programId - the program that owns the accounts we are filtering
     */
    static gpaBuilder(programId = new web3.PublicKey('totoRorVC1BCmAMFvxM55t9RjPMsGeTTVdUgGC9AF8g')) {
        return beetSolana.GpaBuilder.fromStruct(programId, totemBeet);
    }
    /**
     * Deserializes the {@link Totem} from the provided data Buffer.
     * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
     */
    static deserialize(buf, offset = 0) {
        return totemBeet.deserialize(buf, offset);
    }
    /**
     * Serializes the {@link Totem} into a Buffer.
     * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
     */
    serialize() {
        return totemBeet.serialize(Object.assign({ accountDiscriminator: totemDiscriminator }, this));
    }
    /**
     * Returns the byteSize of a {@link Buffer} holding the serialized data of
     * {@link Totem}
     */
    static get byteSize() {
        return totemBeet.byteSize;
    }
    /**
     * Fetches the minimum balance needed to exempt an account holding
     * {@link Totem} data from rent
     *
     * @param connection used to retrieve the rent exemption information
     */
    static getMinimumBalanceForRentExemption(connection, commitment) {
        return __awaiter(this, void 0, void 0, function* () {
            return connection.getMinimumBalanceForRentExemption(Totem.byteSize, commitment);
        });
    }
    /**
     * Determines if the provided {@link Buffer} has the correct byte size to
     * hold {@link Totem} data.
     */
    static hasCorrectByteSize(buf, offset = 0) {
        return buf.byteLength - offset === Totem.byteSize;
    }
    /**
     * Returns a readable version of {@link Totem} properties
     * and can be used to convert to JSON and/or logging
     */
    pretty() {
        return {
            dao: this.dao.toBase58(),
            slotsPerChallengePeriod: (() => {
                const x = this.slotsPerChallengePeriod;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            admin: this.admin.toBase58(),
            statements: (() => {
                const x = this.statements;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            requests: (() => {
                const x = this.requests;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
            totalDisputes: (() => {
                const x = this.totalDisputes;
                if (typeof x.toNumber === 'function') {
                    try {
                        return x.toNumber();
                    }
                    catch (_) {
                        return x;
                    }
                }
                return x;
            })(),
        };
    }
}
/**
 * @category Accounts
 * @category generated
 */
export const totemBeet = new beet.BeetStruct([
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['dao', beetSolana.publicKey],
    ['slotsPerChallengePeriod', beet.u64],
    ['admin', beetSolana.publicKey],
    ['statements', beet.u64],
    ['requests', beet.u64],
    ['totalDisputes', beet.u64],
], Totem.fromArgs, 'Totem');
